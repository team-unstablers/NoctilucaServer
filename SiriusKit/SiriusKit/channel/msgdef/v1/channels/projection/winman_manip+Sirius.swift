//
//  winman_manip+Sirius.swift
//  SiriusKit
//
//  Auto-generated by protoc-gen-sirius. DO NOT EDIT.
//

import Foundation
internal import SwiftProtobuf

public extension MessageOpcode {
    static let windowManipulationRequest: MessageOpcode = MessageOpcode(rawValue: 0x8043)
}

public struct WindowManipulationRequest: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_WindowManipulationRequest
    
    public enum Operation {
        case stateCommand(WindowStateCommand)
        case focus(Bool)
        case setGeometry(SRRect)
        case setFlags(UInt64)
        case clearFlags(UInt64)
    }
    
    public let windowID: UInt64
    public let operation: Operation
    public let extraArgs: [String: String]
    public let flags: UInt32


    public init(windowID: UInt64, operation: Operation, extraArgs: [String: String], flags: UInt32) {
        self.windowID = windowID
        self.operation = operation
        self.extraArgs = extraArgs
        self.flags = flags
    }

    init(from protobufMessage: Sirius_Msgdef_V1_Channels_Projection_WindowManipulationRequest) throws {
        self.windowID = protobufMessage.windowID
        self.extraArgs = protobufMessage.extraArgs
        self.flags = protobufMessage.flags
        switch protobufMessage.operation {
        case .stateCommand(let val):
            self.operation = .stateCommand(.init(rawValue: val))
            break
        case .focus(let val):
            self.operation = .focus(val)
            break
        case .setGeometry(let val):
            self.operation = .setGeometry(SRRect(from: val))
            break
        case .setFlags(let val):
            self.operation = .setFlags(val)
            break
        case .clearFlags_p(let val):
            self.operation = .clearFlags(val)
            break
        case .none:
            throw SiriusMessageError.invalidProtobufMessage
        }
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.windowID = self.windowID
        message.extraArgs = self.extraArgs
        message.flags = self.flags
        switch self.operation {
        case .stateCommand(let val):
            message.operation = .stateCommand(val.rawValue)
            break
        case .focus(let val):
            message.operation = .focus(val)
            break
        case .setGeometry(let val):
            message.operation = .setGeometry(val.toProtobufMessage())
            break
        case .setFlags(let val):
            message.operation = .setFlags(val)
            break
        case .clearFlags(let val):
            message.operation = .clearFlags_p(val)
            break
        }

        return message
    }
}

public struct WindowManipulationResponse: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_WindowManipulationResponse
    
    public let isSuccess: Bool
    public let code: UInt32
    public let message: String?


    public init(isSuccess: Bool, code: UInt32, message: String?) {
        self.isSuccess = isSuccess
        self.code = code
        self.message = message
    }

    init(from protobufMessage: Sirius_Msgdef_V1_Channels_Projection_WindowManipulationResponse) throws {
        self.isSuccess = protobufMessage.isSuccess
        self.code = protobufMessage.code
        self.message = protobufMessage.hasMessage ? protobufMessage.message : nil
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.isSuccess = self.isSuccess
        message.code = self.code
        if let messageText = self.message {
            message.message = messageText
        }

        return message
    }
}
