//
//  accessibility+Constants.swift
//  SiriusKit
//
//  Auto-generated by mdproto-gen-swift. DO NOT EDIT.
//

import Foundation

public struct AccessibilityNodeRole: RawRepresentable, Hashable, Equatable, Codable, Sendable {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    /// A readable text element.
    /// e.g., TextView (Android), Label (Windows), Text (macOS SwiftUI)
    public static let text = AccessibilityNodeRole(rawValue: "text")
    /// A text field element that accepts user input.
    /// e.g., EditText (Android), TextBox (Windows), TextField (macOS SwiftUI)
    public static let textField = AccessibilityNodeRole(rawValue: "textField")
    /// An image element.
    /// e.g., ImageView (Android), PictureBox (Windows), Image (macOS SwiftUI)
    public static let image = AccessibilityNodeRole(rawValue: "image")
    /// A button element.
    /// e.g., Button (Android), Button (Windows), Button (macOS SwiftUI)
    public static let button = AccessibilityNodeRole(rawValue: "button")
    /// A toggle button element (checkbox, radio button, etc.).
    /// e.g., CheckBox, RadioButton (Android), CheckBox, RadioButton (Windows), Toggle (macOS SwiftUI)
    public static let toggle = AccessibilityNodeRole(rawValue: "toggle")
    /// A slider element.
    /// Scroll view scrollbars MAY also be represented as sliders.
    public static let slider = AccessibilityNodeRole(rawValue: "slider")
    /// A combo box element (dropdown menu, etc.).
    /// e.g., Spinner (Android), ComboBox (Windows), Picker (macOS SwiftUI)
    public static let comboBox = AccessibilityNodeRole(rawValue: "comboBox")
    /// A list element (list view, table view, etc.).
    /// e.g., ListView, RecyclerView (Android), ListView, DataGrid (Windows), List (macOS SwiftUI)
    public static let list = AccessibilityNodeRole(rawValue: "list")
    /// A tab element (tab layout, etc.).
    /// e.g., TabLayout (Android), TabControl (Windows), TabView (macOS SwiftUI)
    public static let tab = AccessibilityNodeRole(rawValue: "tab")
    /// A tree element (tree view, etc.).
    /// e.g., TreeView (Windows), OutlineGroup (macOS SwiftUI)
    public static let tree = AccessibilityNodeRole(rawValue: "tree")
    /// A web DOM element. See the 'REPRESENTING DOM ELEMENTS' section of the mdproto file.
    public static let domElement = AccessibilityNodeRole(rawValue: "domElement")
    /// A layout container element.
    /// e.g., LinearLayout, RelativeLayout (Android), StackPanel (Windows), VStack, HStack (macOS SwiftUI)
    public static let layoutContainer = AccessibilityNodeRole(rawValue: "layoutContainer")
    /// A window element.
    public static let window = AccessibilityNodeRole(rawValue: "window")
    /// An individual menu item element.
    public static let menuItem = AccessibilityNodeRole(rawValue: "menuItem")
    /// A logical group of menu items.
    public static let menuGroup = AccessibilityNodeRole(rawValue: "menuGroup")
    /// A menu container element.
    public static let menu = AccessibilityNodeRole(rawValue: "menu")
    /// An application element.
    public static let application = AccessibilityNodeRole(rawValue: "application")
    /// A display screen element.
    public static let display = AccessibilityNodeRole(rawValue: "display")
    /// A GUI session element.
    public static let session = AccessibilityNodeRole(rawValue: "session")
}

public struct AccessibilityActionType: RawRepresentable, Hashable, Equatable, Codable, Sendable {
    public let rawValue: String

    public init(rawValue: String) {
        self.rawValue = rawValue
    }

    /// Performs the default interaction with the node.
    /// For example, this could be a click for a button, focus for a text field, or selection for a menu item.
    public static let activate = AccessibilityActionType(rawValue: "activate")
    /// Deactivates the interaction with the node.
    /// For example, performing this action on a toggle button that is on MAY turn it off.
    public static let deactivate = AccessibilityActionType(rawValue: "deactivate")
    /// Attempts to open a context menu from the node. Similar to a right-click action.
    public static let contextMenu = AccessibilityActionType(rawValue: "contextMenu")
    /// Explicitly performs a 'click'.
    /// value: intval (optional) - mouse button index (0: left, 1: right, 2: middle)
    public static let click = AccessibilityActionType(rawValue: "click")
    /// Explicitly performs a 'double click'.
    /// value: intval (optional) - mouse button index (0: left, 1: right, 2: middle)
    public static let doubleClick = AccessibilityActionType(rawValue: "doubleClick")
    /// Explicitly performs a 'long press'.
    /// value: intval (optional) - mouse button index (0: left, 1: right, 2: middle)
    public static let longClick = AccessibilityActionType(rawValue: "longClick")
    /// Focuses the node.
    public static let focus = AccessibilityActionType(rawValue: "focus")
    /// Removes focus from the node.
    public static let blurFocus = AccessibilityActionType(rawValue: "blurFocus")
    /// Scrolls the node.
    /// args: "deltaX" (optional) - horizontal scroll amount (negative for left, positive for right)
    ///       "deltaY" (optional) - vertical scroll amount (negative for up, positive for down)
    public static let scroll = AccessibilityActionType(rawValue: "scroll")
    /// Expands the child content of the node.
    public static let expand = AccessibilityActionType(rawValue: "expand")
    /// Collapses the child content of the node.
    public static let collapse = AccessibilityActionType(rawValue: "collapse")
    /// Sets the value of the node.
    /// For example, this could be entering text in a text field or changing the value of a slider.
    public static let setValue = AccessibilityActionType(rawValue: "setValue")

    /// Cancels or dismisses a transient UI element associated with the node.
    /// For example, this could close a popup/context menu, dismiss a modal dialog, or abort an in-progress gesture.
    /// On macOS hosts, this typically maps to the AX `kAXCancelAction`.
    public static let cancel = AccessibilityActionType(rawValue: "cancel")
}
