//
//  displayman_manip+Sirius.swift
//  SiriusKit
//
//  Auto-generated by protoc-gen-sirius. DO NOT EDIT.
//

import Foundation
internal import SwiftProtobuf

public extension MessageOpcode {
    static let displayTransactionRequest: MessageOpcode = MessageOpcode(rawValue: 0x8048)
    static let displayTransactionResponse: MessageOpcode = MessageOpcode(rawValue: 0x8049)
}

public struct DisplayLayoutChange: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_DisplayLayoutChange

    public let displayID: UInt32
    public let spec: DisplaySpec?
    public let rotation: DisplayRotation?
    public let position: SRPoint?


    public init(displayID: UInt32, spec: DisplaySpec?, rotation: DisplayRotation?, position: SRPoint?) {
        self.displayID = displayID
        self.spec = spec
        self.rotation = rotation
        self.position = position
    }

    init(from protobufMessage: ProtobufMessage) throws {
        self.displayID = protobufMessage.displayID
        self.spec = protobufMessage.hasSpec ? try DisplaySpec(from: protobufMessage.spec) : nil
        self.rotation = protobufMessage.hasRotation ? DisplayRotation(rawValue: protobufMessage.rotation) : nil
        self.position = protobufMessage.hasPosition ? SRPoint(from: protobufMessage.position) : nil
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.displayID = self.displayID
        if let spec = self.spec {
            message.spec = spec.toProtobufMessage()
        }
        if let rotation = self.rotation {
            message.rotation = rotation.rawValue
        }
        if let position = self.position {
            message.position = position.toProtobufMessage()
        }

        return message
    }
}

public struct DisplaySetEnable: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_DisplaySetEnable

    public let displayID: UInt32
    public let isEnabled: Bool


    public init(displayID: UInt32, isEnabled: Bool) {
        self.displayID = displayID
        self.isEnabled = isEnabled
    }

    init(from protobufMessage: ProtobufMessage) throws {
        self.displayID = protobufMessage.displayID
        self.isEnabled = protobufMessage.isEnabled
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.displayID = self.displayID
        message.isEnabled = self.isEnabled

        return message
    }
}

public struct VirtualDisplayCreate: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_VirtualDisplayCreate

    public let identifier: UUID
    public let desiredSpecs: [DisplaySpec]
    public let purpose: String?
    public let metadata: [String: String]


    public init(identifier: UUID, desiredSpecs: [DisplaySpec], purpose: String?, metadata: [String: String]) {
        self.identifier = identifier
        self.desiredSpecs = desiredSpecs
        self.purpose = purpose
        self.metadata = metadata
    }

    init(from protobufMessage: ProtobufMessage) throws {
        self.identifier = UUID(msgdef: protobufMessage.identifier)
        self.desiredSpecs = try protobufMessage.desiredSpecs.map { try DisplaySpec(from: $0) }
        self.purpose = protobufMessage.hasPurpose ? protobufMessage.purpose : nil
        self.metadata = protobufMessage.metadata
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.identifier = self.identifier.asMsgDef()
        message.desiredSpecs = self.desiredSpecs.map { $0.toProtobufMessage() }
        if let purpose = self.purpose {
            message.purpose = purpose
        }
        message.metadata = self.metadata

        return message
    }
}

public struct VirtualDisplayDestroy: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_VirtualDisplayDestroy

    public let identifier: UUID


    public init(identifier: UUID) {
        self.identifier = identifier
    }

    init(from protobufMessage: ProtobufMessage) throws {
        self.identifier = UUID(msgdef: protobufMessage.identifier)
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.identifier = self.identifier.asMsgDef()

        return message
    }
}

public struct DisplayOperation: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_DisplayOperation

    public enum OneOf_Operation: Sendable {
        case change(DisplayLayoutChange)
        case setEnable(DisplaySetEnable)
        case createVirtualDisplay(VirtualDisplayCreate)
        case destroyVirtualDisplay(VirtualDisplayDestroy)
        case none
    }

    public let operation: OneOf_Operation


    public init(operation: OneOf_Operation) {
        self.operation = operation
    }

    init(from protobufMessage: ProtobufMessage) throws {
        switch protobufMessage.operation {
        case .change(let val):
            self.operation = .change(try DisplayLayoutChange(from: val))
        case .setEnable(let val):
            self.operation = .setEnable(try DisplaySetEnable(from: val))
        case .createVirtualDisplay(let val):
            self.operation = .createVirtualDisplay(try VirtualDisplayCreate(from: val))
        case .destroyVirtualDisplay(let val):
            self.operation = .destroyVirtualDisplay(try VirtualDisplayDestroy(from: val))
        case .none:
            self.operation = .none
        }
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        switch self.operation {
        case .change(let val):
            message.operation = .change(val.toProtobufMessage())
        case .setEnable(let val):
            message.operation = .setEnable(val.toProtobufMessage())
        case .createVirtualDisplay(let val):
            message.operation = .createVirtualDisplay(val.toProtobufMessage())
        case .destroyVirtualDisplay(let val):
            message.operation = .destroyVirtualDisplay(val.toProtobufMessage())
        case .none:
            break
        }

        return message
    }
}

public struct DisplayTransactionRequest: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_DisplayTransactionRequest

    public let transactionID: UUID
    public let operations: [DisplayOperation]
    public let mainDisplayID: UInt32?


    public init(transactionID: UUID, operations: [DisplayOperation], mainDisplayID: UInt32?) {
        self.transactionID = transactionID
        self.operations = operations
        self.mainDisplayID = mainDisplayID
    }

    init(from protobufMessage: ProtobufMessage) throws {
        self.transactionID = UUID(msgdef: protobufMessage.transactionID)
        self.operations = try protobufMessage.operations.map { try DisplayOperation(from: $0) }
        self.mainDisplayID = protobufMessage.hasMainDisplayID ? protobufMessage.mainDisplayID : nil
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.transactionID = self.transactionID.asMsgDef()
        message.operations = self.operations.map { $0.toProtobufMessage() }
        if let mainDisplayID = self.mainDisplayID {
            message.mainDisplayID = mainDisplayID
        }

        return message
    }
}

public struct DisplayTransactionResponse: SiriusMessage {
    typealias ProtobufMessage = Sirius_Msgdef_V1_Channels_Projection_DisplayTransactionResponse

    public let transactionID: UUID
    public let isSuccess: Bool
    public let reason: String?


    public init(transactionID: UUID, isSuccess: Bool, reason: String?) {
        self.transactionID = transactionID
        self.isSuccess = isSuccess
        self.reason = reason
    }

    init(from protobufMessage: ProtobufMessage) throws {
        self.transactionID = UUID(msgdef: protobufMessage.transactionID)
        self.isSuccess = protobufMessage.isSuccess
        self.reason = protobufMessage.hasReason ? protobufMessage.reason : nil
    }

    func toProtobufMessage() -> ProtobufMessage {
        var message = ProtobufMessage()

        message.transactionID = self.transactionID.asMsgDef()
        message.isSuccess = self.isSuccess
        if let reason = self.reason {
            message.reason = reason
        }

        return message
    }
}
